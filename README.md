The FastAPI application (app/main.py) exposes two main endpoints:
- /slack/events: Handles incoming Slack events and PagerDuty alerts
- /slack/commands: Handles Slack slash commands (for manual intervention)
